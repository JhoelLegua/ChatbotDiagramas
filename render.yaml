services:
  - type: web
    name: ia-app
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false 
      - key: PORT
        fromService:
          type: web
          property: port